import React from "react";

const GraficoComparativo = () => (
    <div className="panel-card">
        <h2>📊 Gráfico Comparativo</h2>
        <p>Comparación entre segmentos, periodos, productos o vendedores.</p>
    </div>
);

export default GraficoComparativo;
